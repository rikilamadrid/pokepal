# Pin this project to the public npm registry. Without this, a machine-level
# ~/.npmrc that defaults to a private registry (e.g. an Azure DevOps feed) leaks
# into package-lock.json's resolved URLs and breaks installs anywhere without
# that registry's auth token (Vercel, CI, other machines) with npm E401.
registry=https://registry.npmjs.org/
